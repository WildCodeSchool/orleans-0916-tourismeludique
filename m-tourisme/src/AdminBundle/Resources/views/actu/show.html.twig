{% extends 'layout.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" type="text/css" href="{{ asset('css/styleShow.css')}}">
{% endblock %}
{% block body %}
    <h1>actu</h1>

    <table>
        <tbody>
            <tr>
                <th>Actu N°</th>
                <td>{{ actu.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ actu.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ actu.contenu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{% if actu.date %}{{ actu.date|date('Y-m-d') }}{% endif %}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{ asset('upload/' ~ actu.image) }}" class="img-responsive img-actu"></td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('actu_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('actu_edit', { 'id': actu.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
